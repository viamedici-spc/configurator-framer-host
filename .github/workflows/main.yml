name: Build and Publish

on: [ push, workflow_dispatch ]

jobs:
    build-publish:
        permissions:
            contents: read
            id-token: write
        uses: viamedici-spc/github-ci/.github/workflows/build-npm.yml@feature/fix-gitversion
        with:
            node-version: '24'
            build-verbs: 'build'
            skip-publish: true

# Test 2